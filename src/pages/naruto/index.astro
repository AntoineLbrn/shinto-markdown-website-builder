---
import Layout from '../../layouts/Layout.astro';
import { getAstroStore, getStoreApiItemSlug } from '../../lib/astroDataStore';

const store = await getAstroStore();
---

<Layout>
    <h1>Naruto Search Engine Page</h1>
    <ul>
        {store.naruto.map((character) => (
            <li>
                <a href={`/naruto/${getStoreApiItemSlug(character)}`}>{character.nameEn}</a>
            </li>
        ))}
    </ul>
</Layout>
