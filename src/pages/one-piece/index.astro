---
import Layout from '../../layouts/Layout.astro';
import { getAstroStore, getStoreItemSlug } from '../../lib/astroDataStore';

const store = await getAstroStore();
---

<Layout>
    <h1>One piece Search Engine Page</h1>
    <ul>
        {store.onePiece.map((character) => (
            <li>
                <a href={`/one-piece/${getStoreItemSlug(character)}`}>{character.nameEn}</a>
            </li>
        ))}
    </ul>
</Layout>
